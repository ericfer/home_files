This repository intendes to keep tracking of my Mac OS X environment

In order to use it, follow the steps bellow:

1) create a directory to download the files. Lets call it "<env_dir>"
2) git clone git://github.com/ericfer/home_files.git <env_dir>
3) cd <env_dir>
4) ./install.sh
5) restart your shell